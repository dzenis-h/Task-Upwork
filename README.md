#### Install `node_modules` insede of the root folder (you must have `NodeJS` installed on your machine)
#### After it's installed, just run `npm start` (insede of the root folder) to run the application.
